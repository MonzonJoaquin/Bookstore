export function Formulario({text, email, password, tel, number, submit, radio, checkbox, range}) {
    return <form onSubmit={submit}>

        <input type="reset" value="" />
        <input type="submit" value="" />
    </form>
}